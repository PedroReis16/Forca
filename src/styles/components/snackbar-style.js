import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: "100%",
    paddingVertical: 16,
    paddingHorizontal: 16,
    position: "absolute",
    bottom: 0,
    zIndex: 10,
  },
  message: {
    color: "#fff",
    fontSize: 14,
  },
});
