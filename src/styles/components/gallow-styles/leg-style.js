import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  leg: {
    position: "absolute",
    width: 40,
    height: 5,
    backgroundColor: "#333",
    transformOrigin: "left center",
  },
});
