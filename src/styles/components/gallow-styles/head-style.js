import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  head: {
    position: "absolute",
    top: 50,
    left: 182.5,
    width: 35,
    height: 35,
    borderRadius: 20,
    borderWidth: 5,
    borderColor: "#333",
  },
});
